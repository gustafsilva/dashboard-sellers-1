import data from './data';

export const INIT_STATE = data;

const suggestions = (state = INIT_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default suggestions;
